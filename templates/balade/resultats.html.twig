{% extends 'base.html.twig' %}

{% block title %}Walk search result{% endblock %}

{% block body %}

    <h1>Upcoming walks in {{ville}}: </h1>
    <div class="future-walks">

    {% for balade in resultatsBalades %}
        
        <div class="walk-box">
            <div class='walk-title'>
                <p><a href="{{ path('show_balade', {'id': balade.id}) }}">{{ balade }}</a></p>
            </div>
            <div class='walk-date'>
                <p>{{balade.dateHeureDepart | date('d-m-Y H:i')}}</p>
            </div>
        </div>

    {% endfor %}
</div>

{% endblock %}